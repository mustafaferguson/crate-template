/// ### Multiply
///
/// A basic function that multiplies two numbers together
///
/// ```rust
/// use crate_template::{multiply};
///
/// let combined = multiply(2, 3);
/// assert_eq!(combined, 6); // true
/// ```
pub fn multiply(a: usize, b: usize) -> usize { a * b }
